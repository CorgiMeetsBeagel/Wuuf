const { query, json } = require('express');
const controller = {};


controller.swipe = async (req, res, next) => {
  //return 20 random userIds
  //that are not already matched or rejected

  


  return next();
}


module.exports = controller;
